<Page x:Class="API_KiluninaAA.pages.Groups"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:API_KiluninaAA.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Groups">

    <Grid Background="white">
        <Grid Height="50" VerticalAlignment="top" Background="#FF0074FF">
            <Label Content="Группы:" Foreground="white" Margin="5" FontSize="20" FontWeight="Bold"/>
        </Grid>
        <ListView
            Margin="10, 87, 10, 10" x:Name="parentGroup">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="№ п/п" Width="50" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Наименование" Width="150" DisplayMemberBinding="{Binding Name}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Label Content="Найти:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,55,0,0" FontWeight="Bold"/>
        <TextBox x:Name="find" TextChanged="FindGroups" Height="25" Margin="70,55,10,0" TextWrapping="Wrap" VerticalAlignment="top"/>
    </Grid>
</Page>
